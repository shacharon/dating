# Admin legacy surfaces

**Product match stack:** `MeMatchesService` (`me-profile/me-matches.service.ts`)  
**Mobile endpoint:** `GET /api/v1/me/matches`

Do **not** extend `MatchesService` or controllers under `admin-legacy/matches/` for product or Android features.

Quarantined in Sprint 64 Story 02.
