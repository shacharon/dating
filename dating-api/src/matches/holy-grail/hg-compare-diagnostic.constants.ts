/**
 * Gates POST `/api/v1/matches/compare/hg-diagnostic`.
 * When unset or not `1`/`true`, the endpoint responds 403 (feature off).
 */
export const ENABLE_HG_COMPARE_DIAGNOSTIC_ENV = 'ENABLE_HG_COMPARE_DIAGNOSTIC';
