# Story 04 — Match display lib + admin RQ

**Sprint 56 · Planned · P1 · ~1.5d · Agent 3.5**

Move `match-display` / prose out of `app/dating/me-matches` into `lib/matches` or `components/match-*`. Admin lists: shared fetch error + RQ for violations/reports; add missing specs.
