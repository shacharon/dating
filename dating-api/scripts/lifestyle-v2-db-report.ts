/**
 * DEPRECATED: legacy lifestyle DB report retired before MatchmakingProfile drop.
 *
 * Kept as fail-fast stub so direct execution is explicit.
 *
 * Run: npm run lifestyle-v2:db-report
 */
function main(): void {
  throw new Error(
    'DEPRECATED: scripts/lifestyle-v2-db-report.ts is retired in pre–Migration 4 tooling cleanup.',
  );
}

main();
