'use client';

/** Stable public path — socket lifecycle. */
export {
  useMessagingSocket,
  type MessagingConnectionStatus,
  type UseMessagingSocketOptions,
} from '@/hooks/messaging/use-messaging-socket';
